# on: push

# jobs:
#   check:
#     runs-on: ubuntu-latest
#     container: leshiy1295/gcc_linters
#     steps:
#       - uses: actions/checkout@v2
#       - run: make check
#   build:
#     runs-on: ubuntu-latest
#     container: leshiy1295/gcc_linters
#     needs: [check]
#     steps:
#     - uses: actions/checkout@v2
#     - run: make build
#     - name: Upload artifacts
#       uses: actions/upload-artifact@v2
#       with:
#         path: main.out
#   test:
#     runs-on: ubuntu-latest
#     container: leshiy1295/gcc_linters
#     needs: [build]
#     steps:
#       - uses: actions/checkout@v2
#       - run: make test
on: push
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@hw-1
      - run: make check
  # run:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name
  #     - name: Fetch
  #       uses: actions/checkout@hw-1
  #     - name: Test
  #       run: |
  #         gcc -ftest-coverage -fprofile-arcs -O0 -o test test.c lib.c
  #         ./test
  #         gcov -abcfu lib.c
  # - name: Upload
  #   uses: codecov/codecov-action@v2
  #   with:
  #     files: ./lib.c.gcov
